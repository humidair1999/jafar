var assert = require('assert'),
    Jafar = require('../index');

describe('TODO', function() {
    it('TODO', function() {
        var jafar = new Jafar({
            json: {
                "thing": {
                    "another": "something"
                }
            }
            // ,
            // json: './sample.json'
        });

        jafar.printJson();

        //assert.equal(TODO, TODO);
    });
});